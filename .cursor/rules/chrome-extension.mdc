---
description: Chrome MV3 extension constraints for Rextensions
globs: apps/extension/public/manifest.json,apps/extension/src/background.ts,apps/extension/vite.config.js,apps/extension/src/utils/chromeGoogleAuth.ts,apps/extension/src/**/*.ts,apps/extension/src/**/*.vue
alwaysApply: false
---

# Chrome extension rules

- Manifest V3 only. Permissions: `management`, `tabs`, `storage`, `identity` plus Google/Firebase host permissions.
- Popup UI is fixed ~400×500 (`App.vue`). Prefer compact layouts.
- Use `createWebHashHistory` — extension pages are not a normal HTTP origin.
- Persist UI path / remote lists via `chrome.storage.local`, not `localStorage`.
- Auth is Google-only: prefer `chrome.identity.getAuthToken`, fall back to `launchWebAuthFlow` (Arc/Brave) → Firebase `signInWithCredential` (`chromeGoogleAuth.ts`). Needs both Chrome-extension and Web OAuth client IDs. Rebuild after changing either.
- Backup/restore syncs an extension **list** (Open Store for missing); does not auto-install.
- After code changes: `npm run build:extension`, then Reload unpacked from `apps/extension/dist`.
- Do not add Node-only APIs to popup/background runtime code.
